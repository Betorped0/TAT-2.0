@model TAT001.Entities.TS_CAMPO

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.pagina_r = 1311;
    ViewBag.carpeta_r = 500;
}

<div class="row">
    <div class="col s12 right-align">
        @*<a href="@Url.Action("Edit", "DesviacionesTS")" class="btn-small">Editar</a>*@
        <a href="@Url.Action("Index", "DesviacionesTS")" class="btn-small">Lista</a>
    </div>
</div>


<div class="row">
    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        foreach (var item in Model.TS_FORMT)
        {
            @Html.HiddenFor(model => item.SPRAS_ID)
        }


        <div class="col s6 ">
            <div class="card-panel">
                <div class="row">
                    @*@foreach (var item in Model.TS_FORMT)
                        {*@
                    <div class="form-horizontal">

                        <div class="form-group">
                            @Html.LabelFor(model => Model.TS_FORMT.Where(a => a.SPRAS_ID.Equals(Session["spras"].ToString())).FirstOrDefault().TXT100)

                            @Html.EditorFor(model => Model.TS_FORMT.Where(a => a.SPRAS_ID.Equals(Session["spras"].ToString())).FirstOrDefault().TXT100, new { htmlAttributes = new { @class = "form-control", @disabled = "@disabled" } })

                            @*@Html.LabelFor(model => item.TXT100, htmlAttributes: new { @class = "control-label col-md-2", @id = "lblSolId", @disabled = "@disabled" })
                                @Html.EditorFor(model => item.TXT100, new { htmlAttributes = new { @class = "form-control", @disabled = "@disabled" } })
                                @Html.ValidationMessageFor(model => item.TXT100, "", new { @class = "text-danger" })*@

                        </div>

                    </div>
                    @*}*@
                </div>
            </div>
        </div>


        <div class="col s6 ">
            <div class="card-panel">
                <h5 id="hdTr">Traducciones</h5>
                <br />
                <div class="row">

                    @*<div class=" input-field col s12">
                            @Html.EditorFor(model => model.TXT100, null, Model.SPRAS_ID, new { htmlAttributes = new { @id = "A" + Model.SPRAS_ID, @readonly = "readonly" } })
                            <label for="SPRAS_ID">@Model.SPRA.DESCRIPCION</label>
                        </div>*@
                    @foreach (var item in Model.TS_FORMT)
                    {
                        <div class=" input-field col s12">

                            @Html.EditorFor(modelItem => item.TXT100, new { htmlAttributes = new { @disabled = "disabled" } })
                            <label for="TXT100">@item.SPRA.DESCRIPCION</label>

                        </div>
                    }
                    @*<div class=" input-field col s12 pr1">
                            @Html.Editor("Editor", null, "txval", new { htmlAttributes = new { @readonly = "readonly" } })
                            <label for="Editor">@Model.TXT100</label>
                        </div>*@
                </div>
                <div>
                    @Html.LabelFor(model => model.ACTIVO, htmlAttributes: new { @class = "control-label col-md-2", @id = "lblObliga" })

                    @if ((bool)Model.ACTIVO == true)
                    {
                        <i class='material-icons green-text'>check</i>}
                    else
                    {
                        <i class='material-icons red-text'>clear</i>}

                </div>
            </div>
        </div>
        @*<div class="col s6 ">
                <div class="card-panel">
                    <h5>Soportes</h5>
                    <div class="row">
                        <div><a href="@Url.Action("Index", "CONSOPORTE", new { tsol = Model.ID})" class="btn-small">Soportes</a></div>
                    </div>
                </div>
            </div>*@
    }
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = "lblEditar" }) |
    @Html.ActionLink("Back to List", "Index", null, new { id = "lblRegresar" })
</p>

